overwrite: true
schema: http://localhost:3000/api/graphql # Replace with your GraphQL endpoint URL
documents: 'src/**/*.tsx' # Replace with the path to your GraphQL document files
generates:
  src/generated/graphql.tsx:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-react-apollo'
